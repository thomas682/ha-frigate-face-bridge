services:
  frigate-face-bridge:
    build:
      context: ../frigate-face-bridge
    container_name: frigate-face-bridge
    restart: unless-stopped
    ports:
      - "8099:8099"
    volumes:
      - ./data:/data
      - ../VERSION:/app/VERSION:ro
